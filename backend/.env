# MongoDB Atlas Connection (already configured)
MONGODB_URI=mongodb+srv://monkeiydluffy752:hdx2lkDWkNhNOggC@cluster0.jtvkt.mongodb.net/stealed_data?appName=Cluster0

# Server Configuration
PORT=8080

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345

# Frontend URL (for CORS - add your Vercel URL after deployment)
FRONTEND_URL=http://localhost:5173

# Admin Password Setup Instructions:
# 1. Start server: npm start
# 2. Setup password: curl -X POST http://localhost:8080/api/auth/setup -H "Content-Type: application/json" -d '{"password": "your_chosen_password"}'
# 3. Login test: curl -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"password": "your_chosen_password"}'
# (Remove these after initial setup)
